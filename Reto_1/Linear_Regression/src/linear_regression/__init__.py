"""Reto 1 · T1 — Redefinición de los datos + regresión lineal sin framework.

Paquete ejecutable por módulos (`python -m linear_regression.<módulo>`):

- `build_dataset`: `Data/*.npy` → `dataset/rehab_windows.csv` + diccionario + meta.
- `train`: entrena la regresión lineal multisalida por descenso de gradiente.
- `predict`: predice la actividad de una repetición cruda.
"""
